#include <converter.h>
#include <stdio.h>

size_t utf16_to_utf8(
    uint16_t const* utf16, size_t utf16_len, 
    uint8_t* utf8,         size_t utf8_len
) 
{
    fprintf(stderr, "NOT IMPLEMENTED YET");
    return 0;
}


size_t utf8_to_utf16(
    uint8_t const* utf8, size_t utf8_len, 
    uint16_t* utf16,     size_t utf16_len
) 
{
    fprintf(stderr, "NOT IMPLEMENTED YET");
    return 0;
}